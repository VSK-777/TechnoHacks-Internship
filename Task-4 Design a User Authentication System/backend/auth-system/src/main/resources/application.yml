server:
  port: "${PORT:8080}"

spring:
  application:
    name: "auth-system"
  datasource:
    url: "${NEON_DB_URL:jdbc:postgresql://ep-aged-sound-aonv3kdl-pooler.c-2.ap-southeast-1.aws.neon.tech/neondb?sslmode=require}"
    username: "${NEON_DB_USERNAME:neondb_owner}"
    password: "${NEON_DB_PASSWORD:npg_b6YPrmeAqV5F}"
    driver-class-name: "org.postgresql.Driver"
  jpa:
    hibernate:
      ddl-auto: "update"
    show-sql: true
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.PostgreSQLDialect"

jwt:
  secret: "${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}"
