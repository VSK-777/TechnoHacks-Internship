spring:
  datasource:
    url: "${NEON_DB_URL}"
    username: "${NEON_DB_USERNAME}"
    password: "${NEON_DB_PASSWORD}"
  jpa:
    hibernate:
      ddl-auto: "validate"
    show-sql: false

jwt:
  secret: "${JWT_SECRET}"
